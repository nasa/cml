# This tests the mechanism that prevents having two independent variables with
# the same name.
print("*** TEST: Create an independent variable with the same name ***")
test.duplicate_name()
trick.stop(0)
