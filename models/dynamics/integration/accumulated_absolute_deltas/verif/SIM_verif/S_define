/*****************************************************************************
PURPOSE: (Verify that the AccumualtedAbsoluteDeltas model does actually
          accumulate the absolute-value of changes to an array-variable)
PROGRAMMERS:
    (
     ((Gary Turner) (OSR) (Apr 2017) (Antares)
        (verification test of AccumulatedAbsoluteDeltas model))
    )
*****************************************************************************/

#include "sim_objects/default_trick_sys.sm"
#include "jeod_sys.sm"

/*****************************************************************************
AntaresUnitTestSimObject
Purpose:(The sim)
*****************************************************************************/

// Include headers for classes that this class contains:
##include "cml/models/tools/unit_test/models/include/unit_test.hh"
##include "../../include/accumulated_absolute_deltas.hh"

class AntaresUnitTestSimObject: public Trick::SimObject {
 public:
  // Framework (leave this alone)
  UnitTestFramework framework;

  // Model for testing:
  AccumulatedAbsoluteDeltas accum_val;

  // support variables:
  double vals[4];

  // Uncomment alternate constructor calls if its desired to test
  // the failures of NULL variable or zero size
  AntaresUnitTestSimObject()
    :
    accum_val( vals, 4)
    //accum_val( NULL, 4)
    //accum_val( vals, 0)
    
  {
   // Framework (leave this alone)
   ("initialization")  framework.initialize();
   (1.0, "environment") framework.update();

   // test-specific jobs:
   ("initialization")  accum_val.initialize();
   (1.0, "environment") accum_val.update();
  }

 private:
  AntaresUnitTestSimObject (const AntaresUnitTestSimObject&);
  AntaresUnitTestSimObject & operator = (const AntaresUnitTestSimObject&);

};
AntaresUnitTestSimObject test ;
